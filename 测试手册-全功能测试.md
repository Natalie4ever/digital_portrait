# 员工数字画像系统 — 全功能测试手册


> 测试环境：本地内网单机部署
> 文档版本：1.0 
---

## 目录

1. [测试环境准备](#一测试环境准备)
2. [初始化测试数据](#二初始化测试数据)
3. [模块一：登录与密码](#三模块一登录与密码)
4. [模块二：个人档案](#四模块二个人档案)
5. [模块三：用户管理（管理员）](#五模块三用户管理管理员)
6. [模块四：家访记录](#六模块四家访记录)
7. [模块五：调组管理](#七模块五调组管理)
8. [模块六：智能筛选](#八模块六智能筛选)
9. [模块七：团队能力分析](#九模块七团队能力分析)
10. [模块八：技能标签模板](#十模块八技能标签模板)
11. [模块九：操作日志](#十一模块九操作日志)
12. [权限矩阵验证](#十二权限矩阵验证)
13. [测试执行顺序建议](#十三测试执行顺序建议)

---

## 一、测试环境准备

### 1.1 系统架构说明

本系统分为前端和后端两个服务，测试前需确保两者均已启动：

| 服务 | 默认地址 | 说明 |
|------|----------|------|
| 后端 API | http://localhost:8000 | FastAPI，提供数据接口 |
| 前端页面 | http://localhost:5173 | React，测试人员操作的界面 |
| 数据库 | 本地文件 `digital_portrait.db` | SQLite，无需单独启动 |

### 1.2 启动步骤

**第一步：启动后端**

1. 打开终端（命令提示符或 PowerShell）
2. 进入项目后端目录：`cd <项目路径>/backend`
3. 执行：`python main.py` 或 `uvicorn app.main:app --reload`
4. 看到 `Uvicorn running on http://0.0.0.0:8000` 表示启动成功

**第二步：启动前端**

1. 另开一个终端窗口
2. 进入项目前端目录：`cd <项目路径>/frontend`
3. 执行：`npm run dev`
4. 看到 `Local: http://localhost:5173/` 表示启动成功

**第三步：验证服务正常**

打开浏览器，访问 http://localhost:8000/api/health，若返回 `{"status":"ok"}` 则后端正常。
再访问 http://localhost:5173，若出现登录页面则前端正常。

---

## 二、初始化测试数据

> 在开始任何功能测试前，必须先完成本节的数据初始化，否则后续测试用例将缺少必要的前置条件。

### 2.1 默认管理员账号

系统初始化后自动创建一个管理员账号，无需手动创建：

| 字段 | 值 |
|------|----|
| EHR 号 | 0000001 |
| 初始密码 | 1234567 |
| 角色 | 管理员（admin） |

### 2.2 创建测试账号

使用管理员账号登录后，在「用户管理」页面依次创建以下测试账号：

| 序号 | EHR 号 | 姓名 | 组别 | 角色 | 初始密码 | 用途 |
|------|--------|------|------|------|----------|------|
| 1 | 1000001 | 张组长 | 测试一组 | leader（组长） | 1234567 | 测试组长功能 |
| 2 | 1000002 | 李员工 | 测试一组 | user（普通员工） | 1234567 | 测试普通用户功能 |
| 3 | 1000003 | 王员工 | 测试一组 | user（普通员工） | 1234567 | 测试家访被访人 |
| 4 | 1000004 | 赵员工 | 测试二组 | user（普通员工） | 1234567 | 测试跨组权限 |
| 5 | 1000005 | 陈组长 | 测试二组 | leader（组长） | 1234567 | 测试跨组场景 |

**操作步骤：**
1. 浏览器访问 http://localhost:5173，用 `0000001` / `1234567` 登录
2. 点击左侧菜单「用户管理」
3. 点击右上角「新建用户」按钮
4. 依次填写上表中每个账号的信息，点击「确认」
5. 重复步骤 3-4，直到 5 个账号全部创建完毕

### 2.3 创建技能标签模板

在「技能标签管理」页面创建以下标签（供后续档案和筛选测试使用）：

`Python`、`数据分析`、`项目管理`、`舞蹈`、`主持`、`Java`

**操作步骤：**
1. 点击左侧菜单「技能标签管理」
2. 点击「新建标签」，输入标签名称，点击「确认」
3. 重复直到 6 个标签全部创建

### 2.4 完善测试档案数据

分别用以下账号登录，完善各自档案（用于后续筛选和分析测试）：

**用账号 1000002（李员工）登录，填写：**
- 基础信息：性别「男」，民族「汉族」，职位「柜面服务」
- 通讯信息：手机号 `13800000001`，通勤时间 `20` 分钟，家庭住址「北京市朝阳区测试路1号」
- 技能标签：添加「Python」「数据分析」
- 学历：学历「本科」，毕业院校「测试大学」，毕业时间「2020-06-01」

**用账号 1000003（王员工）登录，填写：**
- 基础信息：性别「女」，职位「客户经理」
- 通讯信息：手机号 `13800000002`，通勤时间 `45` 分钟，家庭住址「北京市海淀区测试路2号」
- 技能标签：添加「舞蹈」「主持」
- 家庭关系：添加一条家庭成员（姓名「王父」，关系「父亲」，工作单位「某公司」）

**用管理员登录，为 1000002 开启应急先锋队标识：**
1. 进入「用户管理」，找到「李员工」
2. 点击「切换应急先锋队」按钮，确认标识变为「是」

### 2.5 数据初始化完成确认

完成以上步骤后，确认以下状态：
- [ ] 用户管理列表中可见 5 个新账号 + 1 个管理员，共 6 条记录
- [ ] 技能标签管理中可见 6 个标签
- [ ] 李员工档案中有技能标签、通讯信息、学历记录
- [ ] 王员工档案中有技能标签、通讯信息、家庭关系记录
- [ ] 李员工的应急先锋队标识为「是」

---

## 三、模块一：登录与密码

### 3.1 正常登录流程

**前置条件：** 系统已启动，浏览器打开 http://localhost:5173

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| AUTH-001 | 管理员正常登录 | 1. 在 EHR 号输入框输入 `0000001`<br>2. 在密码框输入 `1234567`<br>3. 点击「登录」按钮 | 跳转到首页，左侧菜单显示全部管理项（用户管理、档案管理、操作日志等） |
| AUTH-002 | 组长正常登录 | 输入 `1000001` / `1234567`，点击登录 | 跳转首页，左侧菜单显示「档案管理」「家访记录」，不显示「用户管理」「操作日志」 |
| AUTH-003 | 普通员工正常登录 | 输入 `1000002` / `1234567`，点击登录 | 跳转首页，左侧菜单仅显示「我的档案」「家访记录」「修改密码」 |

### 3.2 登录异常场景

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| AUTH-004 | 密码错误 | 输入 `0000001` / `wrongpass`，点击登录 | 页面提示「EHR号或密码错误」，停留在登录页 |
| AUTH-005 | EHR 不存在 | 输入 `9999999` / `1234567`，点击登录 | 页面提示「EHR号或密码错误」 |
| AUTH-006 | EHR 格式错误 | 输入 `123`（不足7位）/ `1234567`，点击登录 | 提示「EHR号或密码错误」 |
| AUTH-007 | 空字段提交 | 不填任何内容，直接点击「登录」 | 提示必填项不能为空 |
| AUTH-008 | 禁用账号登录 | 先用管理员禁用 `1000004`，再用该账号登录 | 提示「账号已禁用」 |

### 3.3 首次登录强制改密

**前置条件：** 账号为新创建或被管理员重置密码后的状态

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| AUTH-009 | 首次登录弹出改密弹窗 | 用新创建的 `1000004` / `1234567` 登录 | 登录后自动弹出「请修改初始密码」对话框，无法关闭或跳过 |
| AUTH-010 | 首次改密成功 | 在弹窗中输入新密码（如 `Test@1234`），确认提交 | 提示「密码修改成功」，弹窗关闭，正常进入系统 |
| AUTH-011 | 新密码与旧密码相同 | 首次改密弹窗中输入与初始密码相同的 `1234567` | 提示「新密码不能与原密码相同」 |

### 3.4 修改密码

**前置条件：** 已正常登录

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| AUTH-012 | 正常修改密码 | 1. 点击左侧「修改密码」<br>2. 输入原密码 `1234567`<br>3. 输入新密码 `NewPass@123`<br>4. 点击「确认修改」 | 提示「密码修改成功」，自动退出登录，跳转登录页 |
| AUTH-013 | 原密码错误 | 修改密码时输入错误的原密码 | 提示「原密码错误」 |
| AUTH-014 | 未登录访问改密页 | 未登录状态下直接访问 `/change-password` | 自动跳转到登录页 |

### 3.5 会话超时

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| AUTH-015 | Token 过期自动登出 | 登录后保持页面不操作超过 1 小时，再点击任意菜单 | 系统清除登录状态，自动跳转到登录页，提示「登录已过期，请重新登录」 |

---

## 四、模块二：个人档案

> 本模块测试每位员工管理自己档案的功能，以及管理员/组长查看他人档案的权限。

### 4.1 查看我的档案

**前置条件：** 以普通员工 `1000002`（李员工）登录

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| PRO-001 | 进入档案页 | 点击左侧菜单「我的档案」 | 页面加载，显示个人基础信息区域及各子模块（学历、家庭、简历等） |
| PRO-002 | 档案内容完整性 | 查看页面各区域 | 可见：基础信息、政治面貌、学历学位、家庭关系、工作简历、奖惩记录、资格证书、专业成果、语言能力、通讯信息、技能标签、发展意向、项目总结 共13个子模块 |

### 4.2 编辑基础信息

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| PRO-003 | 正常更新基础信息 | 1. 点击基础信息区域的「编辑」按钮<br>2. 修改「民族」为「满族」<br>3. 点击「保存」 | 提示「保存成功」，页面刷新后显示更新后的民族 |
| PRO-004 | 身份证号格式校验 | 在身份证号输入框输入 `123456`（不足18位），点击保存 | 提示「证件号码必须为18位」，不允许保存 |
| PRO-005 | 手机号格式校验 | 输入 `12345678901`（非1开头），点击保存 | 提示「手机号格式不正确」 |
| PRO-006 | 日期格式校验 | 在出生日期输入 `2000/13/01`（月份非法），点击保存 | 提示「日期格式不正确」 |

### 4.3 子表记录操作（以学历为例）

> 以下操作步骤适用于所有子表：政治面貌、学历学位、家庭关系、工作简历、奖惩记录、资格证书、专业成果、语言能力

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| PRO-007 | 新增学历记录 | 1. 在「学历学位」区域点击「新增」<br>2. 填写：学历「硕士」，毕业院校「测试大学研究生院」，毕业时间「2023-06-01」<br>3. 点击「确认」 | 新记录出现在学历列表中，按日期排序 |
| PRO-008 | 修改学历记录 | 点击已有学历记录的「编辑」，修改毕业院校名称，保存 | 提示「更新成功」，列表显示新名称 |
| PRO-009 | 删除学历记录 | 点击某条学历记录的「删除」，在确认弹窗中点击「确认」 | 该记录从列表中消失 |
| PRO-010 | 子表日期非法 | 新增学历时，毕业时间输入 `2023-13-01` | 提示「日期格式不正确」 |

### 4.4 通讯信息

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| PRO-011 | 新增/更新通讯信息 | 1. 在「通讯信息」区域点击「编辑」<br>2. 填写手机号 `13800000001`，通勤时间 `20`，家庭住址「北京市朝阳区」<br>3. 点击「保存」 | 提示「保存成功」，通讯信息区域显示填写内容 |
| PRO-012 | 座机格式校验 | 在电话字段输入 `abc-1234`，点击保存 | 提示「电话格式不正确」 |
| PRO-011a | 通勤时间为负数 | 在「通讯信息」编辑时填写通勤时间 `-10`，点击保存 | 提示「通勤时间不能为负数」 |

### 4.5 技能标签

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| PRO-013 | 添加技能标签 | 1. 在「技能标签」区域点击「添加标签」<br>2. 从下拉列表选择「Java」<br>3. 点击「确认」 | 「Java」标签出现在技能标签列表中 |
| PRO-014 | 删除技能标签 | 点击某个标签旁的「×」或「删除」按钮 | 该标签从列表中移除 |

### 4.6 发展意向

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| PRO-015 | 填写发展意向 | 1. 在「发展意向」区域点击「编辑」<br>2. 选择发展路径「管理发展路径」<br>3. 填写短期目标「1年内考取PMP」<br>4. 点击「保存」 | 提示「保存成功」，发展意向区域显示填写内容 |

### 4.7 项目总结

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| PRO-016 | 新增项目总结 | 1. 在「项目总结」区域点击「新增」<br>2. 填写项目名称「测试项目A」，开始时间「2024-01-01」，结束时间「2024-12-31」，角色「测试负责人」<br>3. 点击「确认」 | 新项目记录出现在列表中 |

### 4.8 权限隔离测试

| 用例编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|----------|
| PRO-017 | 普通用户无法查看他人档案 | 以 `1000002` 登录 | 直接在浏览器地址栏访问 `/admin/profile-view/1000003` | 页面提示「无权限」或自动跳转首页 |
| PRO-018 | 组长可查看本组成员档案 | 以 `1000001`（张组长）登录 | 进入「档案管理」，点击「李员工」的「查看」 | 可正常查看李员工档案，但编辑按钮不可用（或无编辑权限） |
| PRO-019 | 组长无法查看他组成员档案 | 以 `1000001`（张组长）登录 | 在地址栏访问 `/admin/profile-view/1000004`（赵员工，测试二组） | 提示「无权限查看」 |
| PRO-020 | 管理员可查看并编辑任意档案 | 以 `0000001` 管理员登录 | 进入「档案管理」，点击任意员工的「查看」，再点击「编辑」 | 可正常查看和编辑 |

---

## 五、模块三：用户管理（管理员）

> 本模块仅管理员可操作，测试前请确保以管理员账号 `0000001` 登录。

### 5.1 用户列表与筛选

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| USR-001 | 查看用户列表 | 点击左侧「用户管理」 | 显示所有用户列表，包含 EHR号、姓名、组别、角色、状态等列 |
| USR-002 | 按组别筛选 | 在筛选栏「组别」输入「测试一组」，点击查询 | 仅显示测试一组的用户 |
| USR-003 | 按角色筛选 | 在「角色」下拉选择「组长」 | 仅显示角色为组长的用户 |
| USR-004 | 显示已禁用用户 | 勾选「包含已禁用」选项 | 列表中出现已禁用账号（灰色显示） |
| USR-005 | 非管理员访问 | 退出登录，用 `1000002` 普通员工登录，访问 `/admin/users` | 自动跳转到首页，不显示用户管理页面 |

### 5.2 创建用户

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| USR-006 | 正常创建用户 | 1. 点击「新建用户」<br>2. 填写 EHR号 `1000006`，姓名「测试用户六」，组别「测试一组」，角色「user」<br>3. 点击「确认」 | 提示「创建成功」，列表中出现新用户 |
| USR-007 | EHR 号重复 | 再次创建 EHR号为 `1000006` 的用户 | 提示「EHR号已存在」，不创建 |
| USR-007a | 软删除后重建 | 先删除用户 `1000006`，再创建相同 EHR 号 `1000006` | 提示「创建成功」，可正常重建 |
| USR-008 | EHR 号格式错误 | 创建时 EHR号输入 `123`（不足7位） | 提示「EHR号必须为7位数字」 |
| USR-009 | 必填项为空 | 不填姓名，直接点击「确认」 | 提示「姓名不能为空」 |

### 5.3 编辑与禁用用户

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| USR-010 | 修改用户信息 | 找到「测试用户六」，点击「编辑」，修改姓名为「测试用户六改」，保存 | 提示「更新成功」，列表显示新姓名 |
| USR-011 | 禁用用户 | 找到 `1000004`（赵员工），点击「禁用」，确认 | 该用户状态变为「已禁用」；用该账号登录时提示「账号已禁用」 |
| USR-012 | 启用用户 | 找到已禁用的 `1000004`，点击「启用」 | 状态恢复为「正常」，可正常登录 |
| USR-013 | 删除用户 | 找到「测试用户六」，点击「删除」，确认 | 该用户从列表消失 |

### 5.4 重置密码

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| USR-014 | 管理员重置密码 | 找到 `1000002`，点击「重置密码」，输入新密码 `Reset@123`，确认 | 提示「密码已重置」；用 `1000002` / `Reset@123` 可登录，且登录后弹出强制改密弹窗 |
| USR-015 | 重置不存在用户 | 通过接口直接请求重置 EHR `9999999` | 返回 404「用户不存在」 |

### 5.5 切换应急先锋队标识

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| USR-016 | 开启应急标识 | 找到 `1000003`（王员工），点击 Switch 开关开启 | 该用户应急先锋队标识变为「是」 |
| USR-017 | 关闭应急标识 | 再次点击 Switch 开关关闭 | 标识变回「否」 |
| USR-018 | 非管理员无法操作 | 以组长身份登录，尝试访问用户管理 | 无法进入用户管理页面 |

### 5.6 批量导入用户

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| USR-019 | 正常批量导入 | 1. 准备 Excel 文件，表头为「姓名、ehr号、组别」，填入2行数据（EHR `1000007`、`1000008`）<br>2. 点击「批量导入」，上传该文件 | 提示「导入完成：新增2条，跳过0条，错误0条」，列表出现新用户 |
| USR-020 | 上传非 Excel 文件 | 上传 `.txt` 文件 | 提示「请上传 Excel 文件（.xlsx）」 |
| USR-021 | Excel 缺少必要列 | 上传只有「姓名」列的 Excel | 提示「Excel 需包含列：姓名、ehr号、组别」 |
| USR-022 | 含重复 EHR 行 | Excel 中某行 EHR 与已有用户重复 | 该行跳过，skipped 计数+1，其余行正常导入 |
| USR-023 | 含格式错误 EHR 行 | Excel 中某行 EHR 为 `123`（不足7位） | 该行记录在 errors 中，其余行正常导入 |

---

## 六、模块四：家访记录

> 家访记录权限规则：组长可新建/编辑本组记录；管理员可查看/删除全部记录；普通员工只能查看自己的记录。

### 6.1 列表与数据范围

**前置条件：** 先用管理员为 `1000003`（王员工）创建一条家访记录（通过后端接口或由组长操作）

| 用例编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|----------|
| HV-001 | 普通员工查看家访列表 | 以 `1000002`（李员工）登录 | 点击左侧「家访记录」 | 仅显示李员工本人的家访记录，不显示他人记录；无「新建」按钮 |
| HV-002 | 组长查看家访列表 | 以 `1000001`（张组长）登录 | 点击「家访记录」 | 显示测试一组所有成员的家访记录；有「新建家访记录」按钮 |
| HV-003 | 管理员查看家访列表 | 以 `0000001` 管理员登录 | 点击「家访记录」 | 显示全部记录；无「新建」按钮；有「删除」按钮 |

### 6.2 筛选功能

**前置条件：** 以管理员登录，系统中已有多条不同年度、不同方式的家访记录

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| HV-004 | 按年度筛选 | 在「年度」输入框输入 `2026`，点击查询 | 仅显示 2026 年度的记录 |
| HV-005 | 按家访方式筛选 | 在「家访方式」下拉选择「实地走访」 | 仅显示实地走访的记录 |
| HV-006 | 按是否已家访筛选 | 选择「已家访」 | 仅显示 is_visited 为 true 的记录 |
| HV-007 | 按被家访人 EHR 筛选 | 输入 `1000003`，点击查询 | 仅显示王员工的家访记录 |
| HV-008 | 分页 | 当记录超过 20 条时，点击第 2 页 | 显示第 2 页数据，分页总数正确 |

### 6.3 新建家访记录（组长）

**前置条件：** 以 `1000001`（张组长）登录，王员工（`1000003`）已填写通讯信息和家庭关系

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| HV-009 | 正常新建家访记录 | 1. 点击「新建家访记录」<br>2. 第一步：选择被家访人「王员工」，填写年度 `2026`，家访时间 `2026-07-01`，家访方式「实地走访」<br>3. 点击「下一步」<br>4. 第二步：确认自动回填的岗位、联系方式、家庭成员信息，填写家访反馈「家庭情况良好」<br>5. 点击「提交」 | 提示「创建成功」，跳转到列表页，新记录可见 |
| HV-010 | 被家访人信息自动回填 | 新建时选择「王员工」 | 岗位、手机号、家庭住址、家属姓名/关系/工作单位自动填入对应字段 |
| HV-011 | 必填项校验 | 不填「被家访人」，直接点击下一步 | 提示「被家访人不能为空」 |
| HV-012 | 必填项校验（年度） | 不填年度，点击下一步 | 提示「年度不能为空」 |
| HV-013 | 非组长无法新建 | 以管理员或普通员工登录，访问 `/home-visits/new` | 页面提示「仅组长可新建或编辑家访记录」 |
| HV-014 | 组长不能为他组成员新建 | 以 `1000001`（张组长）登录，尝试为 `1000004`（赵员工，测试二组）新建 | 提交时提示「只能对本组成员进行家访」 |

### 6.4 编辑家访记录（组长）

**前置条件：** 以 `1000001`（张组长）登录，已有一条本组家访记录

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| HV-015 | 正常编辑 | 1. 在列表点击某条记录的「查看」<br>2. 点击「编辑」<br>3. 修改「家访反馈」内容<br>4. 点击「保存」 | 提示「更新成功」，详情页显示新内容 |
| HV-016 | 编辑时被家访人不可修改 | 进入编辑页面 | 「被家访人」字段显示为只读，无法修改 |
| HV-017 | 管理员不能编辑 | 以管理员登录，进入某条记录详情，点击编辑并提交 | 提示「管理员不可编辑家访记录」，数据不变 |

### 6.5 查看详情与导出

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| HV-018 | 查看详情 | 在列表点击「查看」 | 进入详情页，显示家访明细和完整家访记录表信息 |
| HV-019 | 导出 PDF | 在详情页点击「导出 PDF」 | 浏览器下载 PDF 文件，文件名包含被家访人姓名和日期 |
| HV-020 | 预览打印 | 在详情页点击「预览打印」 | 弹出打印预览窗口，可正常打印 |
| HV-021 | 勾选导出明细表 | 在列表勾选 2 条记录，点击「导出明细表」 | 下载 Excel，仅包含已勾选的 2 条记录 |
| HV-022 | 未勾选导出明细表 | 不勾选任何记录，直接点击「导出明细表」 | 下载 Excel，包含当前页全部记录 |

### 6.6 删除家访记录

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| HV-023 | 组长删除本组记录 | 以组长登录，在列表点击「删除」，确认 | 删除成功，列表不再显示该记录 |
| HV-024 | 管理员删除任意记录 | 以管理员登录，删除任意记录 | 删除成功 |
| HV-025 | 普通员工无删除入口 | 以普通员工登录，查看家访列表 | 列表中无「删除」按钮 |

### 6.7 越权访问异常场景

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| HV-026 | 普通员工查看他人详情 | 以 `1000002` 登录，在地址栏直接访问他人家访记录的详情页 URL | 提示「无权限查看」或跳转首页 |
| HV-027 | 组长查看他组记录详情 | 以 `1000001`（张组长）登录，访问测试二组成员的家访记录详情 | 提示「无权限查看」 |
| HV-028 | 访问不存在的记录 | 在地址栏访问 `/home-visits/99999` | 提示「家访记录不存在」 |

---

## 七、模块五：调组管理

> 本模块仅管理员可操作。

### 7.1 执行调组

**前置条件：** 以管理员 `0000001` 登录

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| TRF-001 | 正常调组 | 1. 进入「档案管理」或「用户管理」，找到 `1000003`（王员工）<br>2. 点击「调组」<br>3. 目标组别选择「测试二组」，填写原因「工作需要」<br>4. 点击「确认」 | 提示「调组成功」；王员工的组别变为「测试二组」；操作日志记录此操作 |
| TRF-002 | 调组后原组长不可见 | 以 `1000001`（张组长）登录，查看档案管理列表 | 王员工不再出现在张组长的列表中 |
| TRF-003 | 调组后新组长可见 | 以 `1000005`（陈组长，测试二组）登录，查看档案管理列表 | 王员工出现在陈组长的列表中 |

### 7.2 查看调组历史

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| TRF-004 | 查看全部调组历史 | 管理员进入「调组管理」页面 | 显示所有调组记录，包含员工姓名、原组、目标组、调组时间、原因 |
| TRF-005 | 按员工筛选历史 | 在筛选栏输入「王员工」或 EHR `1000003` | 仅显示王员工的调组记录 |
| TRF-006 | 查看单员工调组历史 | 在用户管理中找到王员工，点击「调组历史」 | 显示该员工历次调组记录，含入组时间和离组时间 |
| TRF-007 | 非管理员无法访问 | 以组长或普通员工登录，访问调组管理页面 | 跳转首页或提示「无权限」 |

---

## 八、模块六：智能筛选

> 智能筛选支持四大场景，管理员可筛选全员，组长只能筛选本组成员。

**前置条件：** 以管理员 `0000001` 登录，进入「档案管理」→「智能筛选」

### 8.1 应急响应场景

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| SCN-001 | 应急场景筛选 | 1. 选择场景「应急响应」<br>2. 设置最大通勤时间 `60` 分钟<br>3. 点击「查询」 | 返回通勤时间 ≤ 60 分钟的员工列表，应急先锋队员排在前面，非应急队员按通勤时间升序排列 |
| SCN-002 | 无符合条件结果 | 设置最大通勤时间 `5` 分钟 | 若无人通勤 ≤ 5 分钟，显示「暂无符合条件的员工」 |

### 8.2 活动选人场景

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| SCN-003 | 活动场景筛选 | 1. 选择场景「活动选人」<br>2. 兴趣标签选择「舞蹈」「主持」<br>3. 点击「查询」 | 返回持有这两个标签的员工（王员工应出现），匹配度按命中标签数计算 |
| SCN-004 | 单标签匹配 | 只选「舞蹈」 | 返回持有「舞蹈」标签的员工，匹配度 20 |

### 8.3 项目组队场景

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| SCN-005 | 项目场景筛选 | 1. 选择场景「项目组队」<br>2. 必选技能选「Python」「数据分析」<br>3. 最少证书数 `0`，最少项目数 `1`<br>4. 点击「查询」 | 李员工（持有 Python、数据分析标签且有项目总结）应出现在结果中，评分较高 |

### 8.4 人员调配场景

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| SCN-006 | 调配场景筛选 | 1. 选择场景「人员调配」<br>2. 目标组别选「测试一组」<br>3. 最大通勤时间 `60`<br>4. 点击「查询」 | 返回非测试一组的员工，按综合评分排序（应急标识、通勤时间、技能数量影响评分） |

### 8.5 导出筛选结果

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| SCN-007 | 导出 Excel | 完成任意场景筛选后，点击「导出 Excel」 | 下载 Excel 文件，包含 EHR号、姓名、组别、是否应急、通勤时间、技能标签、匹配度等列 |

---

## 九、模块七：团队能力分析

> 管理员可分析全员，组长只能分析本组。

**前置条件：** 以管理员 `0000001` 登录，进入「团队能力分析」页面

### 9.1 综合概览

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| ANA-001 | 查看综合概览 | 进入「团队能力分析」→「综合概览」 | 显示：总人数、各组别人数、应急先锋队人数、技能标签分布图（每项技能多少人掌握） |
| ANA-002 | 技能分布准确性 | 查看「Python」技能的掌握人数 | 应与实际持有该标签的员工数一致（李员工持有，应显示至少 1 人） |

### 9.2 证书统计

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| ANA-003 | 查看证书统计 | 点击「资格证书统计」标签页 | 按资格证书类型显示持有人数和具体人员列表 |

### 9.3 风险预警

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| ANA-004 | 查看风险预警 | 点击「风险预警」标签页 | 显示红/黄/绿三色风险项：仅1人掌握的技能标红，2-3人标黄，4人以上标绿；应急先锋队人数不足3人时标红 |
| ANA-005 | 风险颜色准确性 | 查看「Python」技能的风险色 | 若只有李员工持有，应显示红色（仅1人掌握） |

### 9.4 应急先锋队通勤分布

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| ANA-006 | 查看通勤分布 | 点击「应急能力」标签页 | 显示应急先锋队员的通勤时间分布（<30分钟、30-60分钟、>60分钟、未设置）及覆盖率 |

### 9.5 综合导出

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| ANA-007 | 导出分析报告 | 点击「导出 Excel」 | 下载包含 4 个 Sheet 的 Excel：能力分布、证书统计、风险预警、应急能力 |

---

## 十、模块八：技能标签模板

**前置条件：** 以管理员 `0000001` 登录，进入「技能标签管理」

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| TAG-001 | 查看标签列表 | 进入「技能标签管理」 | 显示所有预定义标签，包含初始化时创建的 6 个标签 |
| TAG-002 | 新建标签 | 点击「新建标签」，输入「测试新标签」，确认 | 新标签出现在列表中 |
| TAG-003 | 标签名称重复 | 再次创建名称为「Python」的标签 | 提示「该标签已存在」，不创建 |
| TAG-004 | 标签名称为空 | 不填名称，直接点击确认 | 提示「名称不能为空」 |
| TAG-005 | 删除标签 | 点击「测试新标签」旁的「删除」，确认 | 该标签从列表消失 |
| TAG-006 | 删除不存在标签 | 通过接口请求删除不存在的 id | 返回 404 |
| TAG-007 | 普通用户可查看标签 | 以普通员工登录，进入「我的档案」→「技能标签」→「添加标签」 | 下拉列表中可见所有预定义标签 |
| TAG-008 | 非管理员无法创建标签 | 以组长登录，访问「技能标签管理」 | 跳转首页或提示「无权限」 |

---

## 十一、模块九：操作日志

**前置条件：** 以管理员 `0000001` 登录，进入「操作日志」

| 用例编号 | 测试项 | 操作步骤 | 预期结果 |
|----------|--------|----------|----------|
| LOG-001 | 查看操作日志 | 点击左侧「操作日志」 | 显示所有操作记录，包含操作人、操作类型、时间等 |
| LOG-002 | 登录操作有记录 | 查看日志列表 | 可找到 action 为 `login` 的记录，对应各账号的登录操作 |
| LOG-003 | 修改密码有记录 | 执行一次修改密码操作后，查看日志 | 出现 action 为 `change_password` 的新记录 |
| LOG-004 | 调组操作有记录 | 执行调组后查看日志 | 出现 action 为 `transfer_user` 的记录 |
| LOG-005 | 按用户筛选 | 在筛选栏输入某用户 EHR，点击查询 | 仅显示该用户的操作记录 |
| LOG-006 | 按操作类型筛选 | 在「操作类型」输入 `login`，点击查询 | 仅显示登录类型的记录 |
| LOG-007 | 分页 | 日志记录较多时，切换页码 | 正确分页显示 |
| LOG-008 | 非管理员无法访问 | 以组长或普通员工登录，访问 `/admin/logs` | 跳转首页或提示「无权限」 |

---

## 十二、权限矩阵验证

> 本节汇总所有关键权限点，建议在完成各模块测试后，集中执行一次权限回归测试。

### 12.1 页面访问权限

| 页面路径 | 管理员 | 组长 | 普通员工 |
|----------|--------|------|---------|
| `/admin/users` 用户管理 | ✅ 可访问 | ❌ 跳转首页 | ❌ 跳转首页 |
| `/admin/profiles` 档案管理 | ✅ 可访问 | ✅ 仅本组 | ❌ 跳转首页 |
| `/admin/logs` 操作日志 | ✅ 可访问 | ❌ 跳转首页 | ❌ 跳转首页 |
| `/admin/skill-tags` 技能标签管理 | ✅ 可访问 | ❌ 跳转首页 | ❌ 跳转首页 |
| `/home-visits` 家访记录 | ✅ 全部记录 | ✅ 本组记录 | ✅ 本人记录 |
| `/home-visits/new` 新建家访 | ❌ 提示无权限 | ✅ 可操作 | ❌ 提示无权限 |
| `/profile` 我的档案 | ✅ 可访问 | ✅ 可访问 | ✅ 可访问 |

### 12.2 关键操作权限

| 操作 | 管理员 | 组长 | 普通员工 |
|------|--------|------|---------|
| 新建/编辑家访记录 | ❌ | ✅（本组） | ❌ |
| 删除家访记录 | ✅（全部） | ✅（本组） | ❌ |
| 切换应急先锋队标识 | ✅ | ❌ | ❌ |
| 执行调组 | ✅ | ❌ | ❌ |
| 创建/删除技能标签模板 | ✅ | ❌ | ❌ |
| 查看他人档案 | ✅（全部） | ✅（本组） | ❌ |
| 重置他人密码 | ✅ | ❌ | ❌ |

---

## 十三、测试执行顺序建议

### 13.1 冒烟测试（约 30 分钟）

优先执行以下用例，快速验证系统核心功能可用：

1. AUTH-001（管理员登录）
2. USR-006（创建用户）
3. PRO-001（查看档案）
4. HV-009（组长新建家访）
5. HV-018（查看家访详情）
6. TAG-001（查看标签列表）
7. LOG-001（查看操作日志）

### 13.2 完整测试顺序

建议按以下顺序执行，确保前置数据满足后续测试需求：

```
第一阶段：环境与数据初始化
  → 第一节：环境准备
  → 第二节：初始化测试数据

第二阶段：基础功能
  → 模块一：登录与密码（AUTH）
  → 模块八：技能标签模板（TAG）
  → 模块二：个人档案（PRO）

第三阶段：管理功能
  → 模块三：用户管理（USR）
  → 模块五：调组管理（TRF）
  → 模块九：操作日志（LOG）

第四阶段：业务功能
  → 模块四：家访记录（HV）
  → 模块六：智能筛选（SCN）
  → 模块七：团队能力分析（ANA）

第五阶段：权限回归
  → 第十二节：权限矩阵验证
```

### 13.3 测试记录表

| 用例编号 | 测试结果 | 实际结果描述 | 测试人 | 测试时间 |
|----------|----------|-------------|--------|----------|
| AUTH-001 | ☐ 通过 ☐ 失败 | | | |
| AUTH-002 | ☐ 通过 ☐ 失败 | | | |
| （依此类推…） | | | | |

---

*文档版本：1.0 | 生成日期：2026-07-29 | 适用系统：员工数字画像系统（Step 1-5 完整功能版）*


